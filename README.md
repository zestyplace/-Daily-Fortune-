# 일진 달력 앱

압축을 풀고 GitHub 저장소에 업로드한 뒤 GitHub Pages로 배포할 수 있습니다.

중요:
- 저장소 이름이 `iljin-app`이 아니면 `vite.config.js`의 `base` 값을 저장소 이름에 맞게 바꾸세요.
  예: 저장소 이름이 `todayun`이면 `base: '/todayun/'`
- GitHub 저장소 Settings → Pages → Source를 `GitHub Actions`로 설정하세요.
